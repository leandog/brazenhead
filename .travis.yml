language: ruby
jdk: oraclejdk7
notifications:
  email:
    - levi@leviwilson.com
    - jeff.morgan@leandog.com
script:
  - ant test
  - bundle install
  - bundle exec rake spec
install:
  - sudo apt-get install -qq --force-yes libgd2-xpm ia32-libs ia32-libs-multiarch
  - ant prepare-travis-ci -q
